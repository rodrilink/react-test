export interface IUser {
  id: string;
  username: string;
}
// Create a function that return a Promise of type IUser array
// The name of the function should be getUsers
// URL: 'https://jsonplaceholder.typicode.com/users'
